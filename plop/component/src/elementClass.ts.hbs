import { {{capitalize componentName}}ElementClass, {{capitalize componentName}}ElementClassProps } from './type';
import clsx from 'clsx';

export const default{{capitalize componentName}}ElementClass = (props: {{capitalize componentName}}ElementClassProps): {{capitalize componentName}}ElementClass => {

  return {
    root: clsx([
      ``
    ])
  }
}

export const tailwind{{capitalize componentName}}ElementClass = (props: {{capitalize componentName}}ElementClassProps): {{capitalize componentName}}ElementClass => {

  return {
    root: clsx([
      ``
    ])
  }
}